#include<bits/stdc++.h>
using namespace std;
vector<int> vec[50];
map<int,string> mm;
map<string, int> mmm;
int main(){
	int n;
	cin>>n;
	int t=0;
	while(n--){
		string s;
		getline(cin,s);
		stringstream strin(s);
		string ss;
		strin>>ss;
		
		vec[++t]
		while(strin>>ss){
			if(mmm[ss]==0){
			mmm[ss]++;
			mm[mmm[ss]]=ss;
		}
		else mm[mmm[ss]]=ss;
			temp = mm[ss];
			vec[t].push_back(temp);
		}
	}
	for(int i=0;i<n;i++){
		for(vector<node>
	}
}
